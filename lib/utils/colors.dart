import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF603814);
  static const Color secondary = Colors.amber;

  // Couleurs de fond
  static const Color cardBackground = Color(0xFFedcfad);
  static const Color navBackground = Color(0xFF402311);

  // Couleurs du texte
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.grey;

  // Couleurs des états
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);

  // Autres
  static const Color border = Color(0xFFc6b8ab);
}
